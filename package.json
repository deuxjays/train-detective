{
	"private": true,
	"name": "train-detective",
	"version": "0.0.0",
	"license": "MIT",
	"scripts": {
		"build": "NODE_OPTIONS=--openssl-legacy-provider preact build",
		"serve": "NODE_OPTIONS=--openssl-legacy-provider sirv build --port 8080 --cors --single",
		"dev": "NODE_OPTIONS=--openssl-legacy-provider preact watch",
		"lint": "eslint src",
		"test": "jest"
	},
	"eslintConfig": {
		"extends": "preact",
		"ignorePatterns": [
			"build/"
		]
	},
	"devDependencies": {
		"@testing-library/jest-dom": "^5.16.4",
		"@testing-library/preact": "^3.2.2",
		"eslint": "^8.17.0",
		"eslint-config-preact": "^1.3.0",
		"eslint-config-prettier": "^8.5.0",
		"eslint-plugin-prettier": "^4.0.0",
		"jest": "^27.5.1",
		"jest-preset-preact": "^4.0.5",
		"preact-cli": "^3.0.0",
		"sass": "^1.53.0",
		"sass-loader": "^10.3.0",
		"sirv-cli": "^2.0.2"
	},
	"dependencies": {
		"preact": "^10.3.2",
		"preact-render-to-string": "^5.1.4",
		"preact-router": "^4.0.1"
	},
	"jest": {
		"preset": "jest-preset-preact"
	},
	"volta": {
		"node": "18.4.0"
	}
}
